export const state = () => ({
  imgURL: process.env.IMAGE_URL
})

export const mutations = {}

export const actions = {}

export const getters = {
  imgURL: (state) => {
    return state.imgURL
  }
}
